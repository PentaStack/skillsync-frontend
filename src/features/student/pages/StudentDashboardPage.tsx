/**
 * StudentDashboardPage — Student workspace.
 *
 * TODO: Implement with:
 * - Booked sessions list (upcoming, past, cancelled)
 * - Cancel / reschedule session actions
 * - View mentor evaluation feedback
 * - Profile quick-edit link
 */
export default function StudentDashboardPage() {
  return (
    <div className="mx-auto max-w-container px-gutter py-8">
      <h1 className="font-display text-display-lg-mobile italic text-text-primary md:text-display-lg">
        Student Dashboard
      </h1>
      <p className="mt-2 font-body text-body-lg text-text-secondary">
        Track your sessions and review feedback
      </p>
      {/* TODO: Dashboard implementation */}
    </div>
  );
}
